using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameController : MonoBehaviour {

    //Publics
    public bool isBattling; //is the battle between player and enemy going at the moment or paused/over

	// Use this for initialization
	void Start () {
        isBattling = true;
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
